#!/usr/bin/make -f
%:
	dh $@ --with=systemd


override_dh_installinit:
	dh_installinit --noscripts

override_dh_systemd_enable:
	dh_systemd_enable --name=pt-desktop-scale-display

override_dh_systemd_start:
	dh_systemd_start --no-start pt-desktop-scale-display